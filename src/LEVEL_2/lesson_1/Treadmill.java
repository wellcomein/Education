package LEVEL_2.lesson_1;

class Treadmill extends Obstacle {
    Treadmill(String name, int distance) {
        this.setName(name);
        this.setRunuble(true);
        this.setDistance(distance);
    }
}
